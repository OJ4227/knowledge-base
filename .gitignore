# Obsidian per-machine UI state
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.obsidian/appearance.json

# Community plugin code — not vendored; install Dataview from the store on each machine
# (see DESIGN.md §10). Committed config (community-plugins.json) records what to install.
.obsidian/plugins/

# JetBrains IDE
.idea/

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*~

# Python tooling
.venv/
__pycache__/
.pytest_cache/
*.pyc
.coverage

# Generated health reports — regenerated by the tools on demand, not versioned
meta/health/lint-report.md
meta/health/report.md

# Note: sources/_snapshots/ IS committed on purpose — archived source content is
# part of the provenance guarantee and must survive link rot.
